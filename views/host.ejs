<!DOCTYPE  html>
<html lang="en" ng-app="host" id="ng-app">
<head>
<meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
<link href="/stylesheets/bootstrap.min.css" rel="stylesheet" type="text/css"/>
<script src="/javascripts/jquery.js" type="text/javascript"></script>
<script src="/javascripts/angular.min.js" type="text/javascript"></script>
<script src="/javascripts/bootstrap.min.js" type="text/javascript"></script>
<script src="/javascripts/host.js" type="text/javascript"></script>
<script>
    window.hosts = <%-JSON.stringify(hosts)%>;
</script>
<!--[if lte IE 8]>
      <script src="/javascripts/json2.js" type="text/javascript"></script>
      <script>
        document.createElement('ng-include');
        document.createElement('ng-pluralize');
        document.createElement('ng-view');
      </script>

<![endif]-->

<title>mongodb viewer base node.js,angularjs,bootstrap</title>
</head>
<body ng-controller="hostCtrl">
	<div class="container">
	    <div class="row">
		  <div class="navbar">
			  <div class="navbar-inner">
			      <% include menu %>
			  </div>
		  </div>
        </div>
		<div class="row">
			<form class="form-horizontal">
			  <fieldset>
			    <legend>添加主机</legend>
			    <div class="control-group " ng-class="{error: nameError==true}">
			      <label class="control-label" for="input01">主机名称</label>
			      <div class="controls">
			        <input type="text" class="input" id="name" ng-model="name"  placeHolder="localhost">
			        <p class="help-block">必须项,仅字母,数字</p>
			      </div>
			    </div>
			    <div class="control-group" ng-class="{error: hostError==true}">
			      <label class="control-label" for="input01">主机地址</label>
			      <div class="controls">
			        <input type="text" class="input-xlarge" id="host" ng-model="host" placeHolder="localhost:27017">
			        <p class="help-block">必须项,支持复制集localhost:27017,localhost:30000</p>
			      </div>
			    </div>
			  </fieldset>
			</form>
            <div class="offset3 span5" >
               <button  class="btn btn-primary" ng-click="create()">保存</button>
	       	   <span　style="color:green">{{notice}}</span>
		    </div>
			<table class="table table-striped table-bordered" >
				<thead>
		          <tr>
		            <th>#</th>
		            <th>主机名</th>
		            <th>主机地址</th>
		            <th>操作</th>
		          </tr>
		        </thead>
		        <tbody>
		          <tr ng-repeat="host in hosts" >
		            <td>{{$index+1}}</td>
		            <td>{{host.name}}</td>
		            <td>{{host.host}}</td>
		            <td><button class="btn btn-danger" ng-click="remove(host.name)">删除</button></td>
		          </tr>
        		</tbody>
			</table>
		</div>
  </div>
</body>

</html>
